import { StyleSheet } from "react-native";
import { screenPadding } from "../../../../../../constants/globalStyles";
export default StyleSheet.create({
    container: {
        flex: 1,
        padding: screenPadding
    }
})